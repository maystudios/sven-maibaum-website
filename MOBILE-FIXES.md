# Mobile Fixes

## 2026-01-29
- Added keyboard focus trap, Esc close, and initial focus for the mobile menu in `src/components/Header.tsx`.
- Enforced 44px touch targets for theme toggle, mobile menu buttons, modal close buttons, and filter buttons in `src/components/ThemeToggle.tsx`, `src/components/Header.tsx`, `src/sections/Philosophy.tsx`, `src/pages/ArchitectShowcase.tsx`, and `src/index.css`.
- Made the Mr. Dork 3 Steam iframe responsive for small screens in `src/pages/projects/ProjectMrDork3.tsx`.
- Increased lightbox control sizes and button hit areas, plus `back-to-portfolio` tap size in `src/index.css`.
